public static class Enums
{
    public enum EPlayer
    {
        Player1,
        Player2
    }
    public enum ELanguage
    {
        EN,
        KR
    }
    public enum EMode
    {
        Single,
        AI,
        MultiWifi
    }
}